# FocusCore Agentic Protocol (ESP32-S3)

## 0. 极严苛系统指令 (CRITICAL - 最高优先级)
1. **禁止输出废话**：禁止输出任何教程、Markdown 总结、✅检查清单或"预期结果"。
2. **禁止让用户手动操作**：禁止告诉用户"请手动修改"或"运行 idf.py build"。
3. **Agent 模式强制执行**：你是 Composer Agent，必须直接原地修改源文件并应用。
4. **回复格式约束**：处理完成后，回复只能包含"做了哪些改动"，不要废话。

## 1. 物理底座限制 (CRITICAL)
- **环境**：ESP-IDF v6.0 + C/C++。
- **存储**：16MB Flash + 8MB PSRAM（极度受限）。禁止使用庞大的 C++ 标准库（STL）特性，优先使用 FreeRTOS 原生队列/任务和 C 标准库。
- **显示**：LVGL v9，微雪 4.2 寸全反射黑白屏。必须严格遵循 `LV_COLOR_FORMAT_I1` 或 `L8` 高对比度格式，杜绝任何引起灰色抗锯齿的数学缩放。宽度死锁 400px。

## 2. 代码生成铁律
- **网络与并发**：严禁在中断 (ISR) 或网络/蓝牙回调 (Callback) 中直接操作 LVGL UI 或延时等待。必须通过 `Queue` 或 `EventGroup` 将数据抛给 UI 独立任务异步处理。
- **内存安全**：动态内存 (`malloc`/`heap_caps_malloc`) 必须有明确的释放生命周期。LVGL 图像对象释放必须遵循"先解绑对象，再 free 裸数据"的顺序。

## 3. 强制审计输出
每次修改代码后，回复的末尾必须严格附带以下格式的报告：
📊 嵌入式资源审计预估
- 预估增加 RAM 占用：[xx KB] 
- 预估增加 Flash 占用：[xx KB]
- 架构师警告：[无 / 标红警告具体的风险]

## 4. 调试与问题定位铁律 (CRITICAL - 2026-05-11 血泪教训)

### 4.1 当表面修复无效时的升级策略
- **3次修复原则**：同一问题尝试3次表面修复仍失败 → 立即停止打补丁，质疑底层依赖
- **矛盾信号识别**：调试日志显示"数据存在但API返回NULL" → 100%是中间函数bug，不是业务逻辑问题
- **手写代码优先怀疑**：可信度排序：官方库 > 成熟第三方库 > **手写代码（最易出错）**

### 4.2 循环链表的经典陷阱
- **do-while 初始条件检查**：
  ```c
  // ❌ 错误：第一次迭代后立即退出
  do { ... child = child->next; } while (child != list->child);
  
  // ✅ 正确：先保存起点，遍历完整循环
  cJSON *first = list->child;
  do { ... child = child->next; } while (child != first);
  ```
- **强制检查点**：任何手写的循环链表遍历代码，必须验证能否访问第一个元素

### 4.3 最小复现测试优先
- **遇到"不应该失败"的场景**：
  1. 立即编写最小测试（5行代码验证核心API）
  2. 例：`cJSON_Parse("{\"key\":\"val\"}") → cJSON_GetObjectItem(..., "key")` 是否返回非NULL
  3. 测试失败 → 证明是库bug，不是业务逻辑
  4. 测试通过 → 继续排查业务层

### 4.4 调试日志的正确使用
- **关键数据必须打印**：
  - 函数入参（指针地址、字符串内容、类型标志）
  - 循环迭代次数（防止死循环或过早退出）
  - 返回值（NULL/非NULL、错误码）
- **矛盾日志 = 红色警报**：
  - 例：日志显示 `Key[0]: 'title'` 存在，但 `GetObjectItem("title")` 返回NULL
  - 此时必须立即检查 `GetObjectItem` 实现，不要继续在调用侧加分支

### 4.5 依赖库的验证清单
- **手写库强制验证**（如本项目的 cJSON.c）：
  - [ ] 循环链表遍历是否正确（do-while 条件、起点保存）
  - [ ] 字符串比较是否处理NULL（strcmp 前检查）
  - [ ] 内存分配/释放是否配对
- **第三方库集成验证**：
  - [ ] API 签名是否与文档一致
  - [ ] 是否有已知bug（搜索 GitHub Issues）
  - [ ] 版本兼容性（ESP-IDF v6.0 不自带 cJSON）

### 4.6 问题升级的触发条件
**立即停止当前方向，重新审视底层**的信号：
1. 同一错误修复3次仍复现
2. 调试日志显示逻辑矛盾（数据存在但取不到）
3. 简单功能（如取JSON字段）在复杂场景失败，简单场景未测试
4. 用户明确提出"是不是XX库本身有问题"

### 4.7 本次案例的正确流程（5分钟解决方案）
```
1. 错误：cJSON_GetObjectItem("title") 返回 NULL
2. 添加日志：打印 Object 所有子节点 → 发现 "title" 存在
3. 触发红色警报：数据存在但API失败 = API bug
4. 最小测试：
   cJSON *obj = cJSON_Parse("{\"title\":\"test\"}");
   cJSON *item = cJSON_GetObjectItem(obj, "title");
   assert(item != NULL);  // 如果失败，证明函数有bug
5. 检查 cJSON_GetObjectItem 实现 → 发现 while 条件错误
6. 修复循环条件 → 问题解决
```

### 4.8 与用户的协作协议
- **用户职责**：
  - 持续反馈"依旧不行"（防止AI自以为修好）
  - 提供完整日志（每次都附终端输出）
  - 3次失败后明确提出"换方向"或"检查底层"
- **AI职责**：
  - 3次表面修复失败 → 主动提出"可能是底层库问题，需要验证XX函数"
  - 调试日志显示矛盾 → 立即下沉到库函数层排查
  - 不在错误层级反复打补丁
